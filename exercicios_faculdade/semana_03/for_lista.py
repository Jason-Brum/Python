numeros = [10, 25, 1, 40, -4]
soma = 0
for num in numeros:  #num aqui, é uma variável que se encontra dentro da lista, que é outra variável, chamada numeros.
    print(num)
    soma = soma + num
print("A soma dos números é:", soma)